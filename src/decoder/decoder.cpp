#include "decoder.hpp"

// Decoder is currently header-only (constructor is defined inline); this
// translation unit exists so the build has a stable object file for the
// base class and a home for any future non-inline members.
